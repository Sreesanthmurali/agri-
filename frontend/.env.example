# Copied to Vercel → Settings → Environment Variables (Production / Preview).
# Full URL of your deployed API including the route prefix, no trailing slash.
# Example: https://your-api.onrender.com/_/backend
VITE_BACKEND_PREFIX=https://YOUR-BACKEND-HOST/_/backend

VITE_SENSOR_PROXY=true
